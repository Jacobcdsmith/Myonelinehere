# Futuristic UI Design

A modern React application built with Vite, featuring a futuristic UI design.

## 🚀 Deployment

This project is automatically deployed to GitHub Pages when changes are pushed to the `main` branch.

**Live Site:** [https://jacobcdsmith.github.io/](https://jacobcdsmith.github.io/)

### How it Works

The deployment is handled by GitHub Actions:
- On every push to `main`, the workflow builds the application
- The built files are deployed to GitHub Pages
- The site is available at the URL above

### Manual Deployment

You can also trigger a deployment manually:
1. Go to the "Actions" tab in the GitHub repository
2. Select "Deploy to GitHub Pages" workflow
3. Click "Run workflow"

## 🛠️ Development

### Prerequisites

- Node.js 20.x or higher
- npm

### Getting Started

1. Clone the repository:
   ```bash
   git clone https://github.com/Jacobcdsmith/Myonelinehere.git
   cd Myonelinehere
   ```

2. Install dependencies:
   ```bash
   npm install --ignore-scripts
   ```

3. Start the development server:
   ```bash
   npm run dev
   ```

4. Open your browser and navigate to `http://localhost:3000`

### Build

To create a production build:

```bash
npm run build
```

The built files will be in the `build` directory.

## 📝 License

This project is private.

